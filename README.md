# Client Connect 2026 Demo

Dummy-data demonstration version.

Run locally:
`python -m streamlit run app.py`

The app contains one attendee per client email. There is no guest name or attendee-count field.
